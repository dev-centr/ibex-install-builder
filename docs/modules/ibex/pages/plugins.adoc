= Plugins
:description: Built-in installer backends and GUI discovery.
:navtitle: Plugins

[cols="1,2,2"]
|===
| Id | Role | Tool / GUI

| `portable-zip`
| Always-on zip payload
| Built-in

| `nsis`
| Emit `.nsi`; build with `makensis`
| link:https://nsis.sourceforge.io/Download[NSIS]

| `inno`
| Emit `.iss`; build with ISCC
| link:https://jrsoftware.org/isdl.php[Inno Setup]

| `msi` / `msix`
| Invoke xref:tools:msi-generator:index.adoc[msi-generator]
| Set `MSI_GENERATOR` or put `msi-generator` on PATH

| `appimage`
| Linux AppDir skeleton / `appimagetool`
| link:https://github.com/AppImage/appimagetool[appimagetool]
|===

Discover, install, and open installer GUIs:

[source,bash]
----
ibex plugins info inno
ibex plugins describe inno
ibex plugins install-scriptbook
ibex plugins install-tool inno
ibex plugins open-gui inno .
ibex plugins install-gui inno
----

`install-tool` runs the plugin's Scriptbook playbook. `open-gui` emits sources and opens the designer on that file. `install-gui` only opens the vendor download page.

Type-specific fields live in `installer.kdl` `extras { }` — see xref:howto-plugin-tools.adoc[How-to: plugin tools].

== Install Coordinator / UIL

Builtin plugin ids uil and install-coordinator emit Unity Install Layout manifests and Install Coordinator job JSON. See xref:install-coordinator-uil.adoc[Install Coordinator / UIL].
